<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:opencv="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Vista de cámara OpenCV (ocupa toda la pantalla) -->
    <org.opencv.android.JavaCameraView
        android:id="@+id/camera_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        opencv:camera_id="any"
        opencv:show_fps="false" />

    <!-- Botón flotante para cambiar de cámara (esquina superior derecha) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btn_switch_camera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:contentDescription="Cambiar cámara"
        app:srcCompat="@drawable/ic_switch_camera"
        app:tint="@color/md_theme_light_onPrimaryContainer"
        app:fabSize="normal"
        app:elevation="6dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Overlay con contenido sobre la cámara -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Card de detección con transparencia (en la parte superior) -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/detection_card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:visibility="gone"
            android:alpha="0.95"
            app:cardElevation="12dp"
            app:cardCornerRadius="24dp"
            app:cardBackgroundColor="@color/md_theme_light_primaryContainer"
            app:strokeWidth="2dp"
            app:strokeColor="@color/md_theme_light_primary">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="24dp"
                android:gravity="center">

                <!-- Imagen de la seña detectada -->
                <ImageView
                    android:id="@+id/iv_sign_image"
                    android:layout_width="160dp"
                    android:layout_height="160dp"
                    android:scaleType="fitCenter"
                    android:contentDescription="Imagen de seña detectada"
                    android:layout_marginBottom="16dp"
                    android:src="@drawable/ic_letra_a" />

                <!-- Texto "Letra X" o "Número X" -->
                <TextView
                    android:id="@+id/tv_sign_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Letra A"
                    android:textSize="32sp"
                    android:textColor="@color/md_theme_light_onPrimaryContainer"
                    android:fontFamily="sans-serif-bold"
                    android:gravity="center" />

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- Espaciador flexible para empujar contenido inferior -->
        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Instrucciones semitransparentes (en la parte inferior) -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:layout_marginBottom="16dp"
            android:alpha="0.90"
            app:cardElevation="4dp"
            app:cardCornerRadius="16dp"
            app:cardBackgroundColor="@color/md_theme_light_surfaceVariant"
            app:strokeWidth="0dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="👋 Muestra una seña\n\nLetras: A, E, I, O, U\nNúmeros: 1, 2, 3"
                android:textSize="14sp"
                android:textColor="@color/md_theme_light_onSurfaceVariant"
                android:gravity="center"
                android:fontFamily="sans-serif-medium"
                android:padding="16dp"
                android:lineSpacingExtra="2dp" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Botón de salir -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_back"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:layout_marginBottom="8dp"
            android:text="Salir"
            android:textSize="16sp"
            app:icon="@android:drawable/ic_menu_close_clear_cancel"
            app:iconGravity="start"
            app:cornerRadius="16dp"
            style="@style/Widget.Material3.Button.TonalButton" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
