<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:opencv="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md_theme_light_background">

    <!-- Contenedor principal con distribución horizontal -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Sección IZQUIERDA: Vista de cámara cuadrada -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="#000000"
            android:padding="16dp">

            <!-- Vista de cámara OpenCV CUADRADA (centrada verticalmente) -->
            <org.opencv.android.JavaCameraView
                android:id="@+id/camera_view"
                android:layout_width="0dp"
                android:layout_height="0dp"
                app:layout_constraintDimensionRatio="1:1"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                opencv:camera_id="any"
                opencv:show_fps="false" />

            <!-- Botón flotante para cambiar de cámara (sobre la cámara) -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/btn_switch_camera"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="12dp"
                android:contentDescription="Cambiar cámara"
                app:srcCompat="@drawable/ic_switch_camera"
                app:tint="@color/md_theme_light_onPrimaryContainer"
                app:fabSize="mini"
                app:elevation="6dp"
                app:layout_constraintTop_toTopOf="@id/camera_view"
                app:layout_constraintEnd_toEndOf="@id/camera_view" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Sección DERECHA: Detección e instrucciones -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:padding="12dp"
            android:gravity="center">

            <!-- Card de detección (más compacta) -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/detection_card"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:visibility="gone"
                app:cardElevation="8dp"
                app:cardCornerRadius="20dp"
                app:cardBackgroundColor="@color/md_theme_light_primaryContainer"
                app:strokeWidth="2dp"
                app:strokeColor="@color/md_theme_light_primary">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="20dp"
                    android:gravity="center">

                    <!-- Imagen de la seña detectada -->
                    <ImageView
                        android:id="@+id/iv_sign_image"
                        android:layout_width="150dp"
                        android:layout_height="150dp"
                        android:scaleType="fitCenter"
                        android:contentDescription="Imagen de seña detectada"
                        android:layout_marginBottom="6dp"
                        android:src="@drawable/ic_letra_a" />

                    <!-- Texto "Letra X" o "Número X" -->
                    <TextView
                        android:id="@+id/tv_sign_label"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Letra A"
                        android:textSize="28sp"
                        android:textColor="@color/md_theme_light_onPrimaryContainer"
                        android:fontFamily="sans-serif-bold"
                        android:gravity="center" />

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- Espaciador -->
            <View
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <!-- Instrucciones -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                app:cardElevation="2dp"
                app:cardCornerRadius="16dp"
                app:cardBackgroundColor="@color/md_theme_light_surfaceVariant">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Letras: A, E, I, O, U\nNúmeros: 1, 2, 3"
                    android:textSize="13sp"
                    android:textColor="@color/md_theme_light_onSurfaceVariant"
                    android:gravity="center"
                    android:fontFamily="sans-serif-medium"
                    android:padding="5dp"/>

            </com.google.android.material.card.MaterialCardView>

            <!-- Botón de salir -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_back"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Salir"
                android:textSize="15sp"
                app:icon="@android:drawable/ic_menu_close_clear_cancel"
                app:iconGravity="start"
                app:cornerRadius="14dp"
                style="@style/Widget.Material3.Button.TonalButton" />

        </LinearLayout>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
